{
	"name": "wp-swiper",
	"version": "1.0.0",
	"description": "",
	"main": "src/index.js",
	"scripts": {
		"start": "wp-scripts start",
		"build": "wp-scripts build",
		"test": "echo \"Error: no test specified\" && exit 1",
		"package": "npm run build && gulp zip",
		"publish:svn": "npm run build && gulp svn"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@wordpress/icons": "^10.32.0",
		"@wordpress/scripts": "^30.25.0",
		"classnames": "^2.3.2",
		"del": "^6.1.1",
		"gulp": "^4.0.2",
		"gulp-vinyl-zip": "^2.5.0"
	},
	"dependencies": {
		"slugify": "^1.6.6",
		"striptags": "^3.2.0"
	}
}
