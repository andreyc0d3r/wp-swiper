{
	"name": "wp-swiper",
	"version": "1.4.4",
	"description": "Swiper JS as a Gutenberg Block - The most modern mobile touch slider with hardware accelerated transitions",
	"main": "src/index.js",
	"scripts": {
		"start": "wp-scripts start",
		"build": "wp-scripts build",
		"test": "echo \"Error: no test specified\" && exit 1",
		"package": "npm run build && gulp zip",
		"publish:svn": "npm run build && gulp svn"
	},
	"author": "Digital Apps <support@digitalapps.com>",
	"license": "GPL-2.0+",
	"devDependencies": {
		"@wordpress/icons": "^11.3.0",
		"@wordpress/scripts": "^31.1.0",
		"classnames": "^2.3.2",
		"del": "^6.1.1",
		"gulp": "^5.0.1",
		"gulp-vinyl-zip": "^2.5.0"
	},
	"dependencies": {
		"slugify": "^1.6.6",
		"striptags": "^3.2.0"
	}
}
